import React from 'react';

const Home = () => {
    return (
        <section id="home" className="section bg-primary">
            <div className="container">
                <h1>Bienvenido a Appatec.cl</h1>
                <p>Explora y descarga la aplicación ACG (Appa Contract Generator) para facilitar la creación y gestión de contratos.</p>
            </div>
        </section>
    );
};

export default Home;
